const Events = require('./events.const')
const Messages = require('./messages.const')
const SERVER = 'server'
const TEST_SERVER = 'ws://localhost:40405'

module.exports = {
    TEST_SERVER,
    SERVER,
    Events,
    Messages
}